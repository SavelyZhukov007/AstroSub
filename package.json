{
  "name": "submind-mobile-shell",
  "version": "0.3.0",
  "private": true,
  "description": "Capacitor WebView shell for Submind LAN client.",
  "scripts": {
    "cap:add:android": "cap add android",
    "cap:sync": "cap sync android",
    "android:debug": "cap add android || true && cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
